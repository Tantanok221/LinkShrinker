<div id="analytics" class="p-6 space-y-8 min-h-screen">
  <%= render Analytic::HeaderComponent.new(short_link: short_link, total_clicks: total_clicks) %>

  <%= render Analytic::TimeComponent.new(
    daily: time_data[:daily],
    weekly: time_data[:weekly],
    monthly: time_data[:monthly]
  ) %>

  <%= render Analytic::GeographyComponent.new(
    country: geo_data[:country],
    region: geo_data[:region],
    city: geo_data[:city]
  ) %>

  <%= render Analytic::ReferrerComponent.new(data: referrer_data) %>
</div>